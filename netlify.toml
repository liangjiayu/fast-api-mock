[build]
  command = "corepack enable && pnpm install && mkdir -p public"
  functions = "netlify/functions"
  publish = "public"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
